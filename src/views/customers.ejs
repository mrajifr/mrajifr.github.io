<% include partials/_header %>
<div class="container mt-5">
  <div> List:
  </div>
  <div class="row">
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <td>No.</td>
            <td>Nama</td>
            <td>Jabatan</td>
            <td>Divisi</td>
            <td>Actions</td>
          </tr>
        </thead>
        <tbody>
          <% if (data) { %>
            <% for(var i = 0; i < data.length; i++) { %>
              <tr>
                <td><%= (i + 1) %></td>
                <td><%= data[i].fullname %></td>
                <td><%= data[i].jabatan %></td>
                <td><%= data[i].divisi%></td>
                <td>
                  <a href="/update/<%= data[i].id %>" class="btn btn-info">Edit</a>
                  <a href="/delete/<%= data[i].id %>" class="btn btn-danger">Delete</a>
                </td>
              </tr>
            <% } %>
          <% } %>
        </tbody>
      </table>
      <div>

        Note:
        <br><br>
        - Server Database menggunakan server database online
        <br>
        - Silahkan masukan data pada kolom-kolom registrasi untuk ditampilkan di list
        <br>
        - Aplikasi ini masih dalam tahap penyempurnaan, Terima Kasih.
        <br>
        - Website Porto Folio Pencipta di https://baca-doang.herokuapp.com
      </div>

    </div>
    <div class="col-md-5">
      <div class="card-body">

        <form action="/add" method="POST">
          <div class="form-group">
            <input type="text" name="username" placeholder="User ID" class="form-control" autofocus>
          </div>
          <div class="form-group">
            <input type="text" name="fullname" placeholder="Nama" maxlength="35" class="form-control" autofocus>
          </div>
          <div class="form-group">
            <input name="password" placeholder="Sandi" type="password" class="form-control" id="exampleInputPassword1" autofocus>
          </div>
          <div class="form-group">
            <input name="confirm" placeholder="Konfirmasi Sandi" type="password" class="form-control" id="exampleInputPassword1" autofocus>
          </div>
          <div class="form-group">
            <input type="text" name="jabatan" placeholder="Jabatan" maxlength="20" class="form-control" autofocus>
          </div>
          <div class="form-group">
            <select name="divisi" placeholder="Divisi" class="form-control">
              <option value="Developer">Developer</option>
              <option value="HRD">HRD</option>
              <option value="Marketing">Marketing</option>
              <option value="Sales">Sales</option>
            </select>
          </div>
          <div class="form-group">
            <input type="text" name="alamat" placeholder="Alamat" maxlength="10" class="form-control" autofocus>
          </div>

          <button type="submit" class="btn btn-primary">
            Registrasi
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<% include partials/_footer %>
